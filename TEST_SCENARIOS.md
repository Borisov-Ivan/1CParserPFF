# Тестовые сценарии для эталонного замера PFF

## Цель

Создание эталонного замера производительности 1С, который покрывает все ключевые кейсы для анализа контекста выполнения (Client/Server/Background) и позволяет точно определить формат PFF.

## Структура конфигурации

### 1. Общие модули

#### `Клиент` (Клиент)
**Файл:** `xml/md/CommonModules/Клиент/Ext/Module.bsl`

**Процедуры:**
- `ВыполнитьКлиентскийКод()` - простая клиентская процедура с циклом (1000 итераций)

**Назначение:** Проверка маркировки чисто клиентского кода в PFF (Client=1, Server=0, Ext=0).

#### `Сервер` (Сервер)
**Файл:** `xml/md/CommonModules/Сервер/Ext/Module.bsl`

**Процедуры:**
- `ВыполнитьСерверныйКод()` - серверная процедура с вложенным вызовом `КлиентСервер.ОбщийКод()`
- `ВызватьИсключение()` - генерация исключения для тестирования обработки ошибок
- `ТестТранзакции()` - работа с транзакцией (НачатьТранзакцию/ЗафиксироватьТранзакцию)
- `РегламентноеЗадание()` - код для фонового задания с записью в журнал регистрации

**Назначение:** Проверка маркировки серверного кода в различных контекстах.

#### `КлиентСервер` (КлиентСервер)
**Файл:** `xml/md/CommonModules/КлиентСервер/Ext/Module.bsl`

**Процедуры:**
- `ОбщийКод()` - общая процедура, вызываемая из разных контекстов
- `ВспомогательнаяПроцедура()` - вложенная процедура для проверки уровней

**Назначение:** Проверка поведения общих модулей при вызове с клиента и с сервера.

### 2. Обработка

#### `ОбработкаТест` → Форма `Форма`
**Файл:** `xml/md/DataProcessors/ОбработкаТест/Forms/Форма/Ext/Form/Module.bsl`

**Команды:**

##### `Тест` (клиентская)
Главный тестовый сценарий, выполняет последовательность вызовов:
1. `Клиент.ВыполнитьКлиентскийКод()` - проверка клиентского контекста
2. `КлиентСервер.ОбщийКод()` - общий модуль из клиентского контекста
3. `ТестНаСервере()` - переход на сервер
4. Внутри `ТестНаСервере()`:
   - `Сервер.ВыполнитьСерверныйКод()` - серверный код
   - `Сервер.ТестТранзакции()` - транзакция
   - `Попытка-Исключение` с `Сервер.ВызватьИсключение()`

##### `СтартоватьФоновое` (клиентская)
Запускает фоновое задание через `ФоновыеЗадания.Выполнить()` с методом `Сервер.РегламентноеЗадание`.

**Процедуры:**
- `ТестНаСервере()` (&НаСервере) - серверная процедура, вызываемая с клиента
- `СтартоватьФоновоеНаСервере()` (&НаСервере) - запуск фонового задания

## Покрытие тестовых кейсов

### Контексты выполнения
- ✅ **Клиент** (Client=1, Server=0, Ext=0): `Клиент.ВыполнитьКлиентскийКод()`
- ✅ **Сервер** (Client=0, Server=1, Ext=0): `Сервер.ВыполнитьСерверныйКод()`
- ✅ **Клиент → Сервер** (Client=1, Server=1, Ext=1): `ТестНаСервере()` из формы
- ✅ **Фоновое задание** (контекст сервера, но отдельный сеанс): `Сервер.РегламентноеЗадание()`
- ✅ **Общий модуль из разных контекстов**: `КлиентСервер.ОбщийКод()`

### Специальные случаи
- ✅ **Обработка исключений**: `Попытка-Исключение` с `Сервер.ВызватьИсключение()`
- ✅ **Транзакции**: `НачатьТранзакцию()` / `ЗафиксироватьТранзакцию()`
- ✅ **Вложенные вызовы**: `КлиентСервер.ОбщийКод()` → `ВспомогательнаяПроцедура()`
- ✅ **Журнал регистрации**: `ЗаписьЖурналаРегистрации()` в фоновом задании

## Инструкция по запуску

1. **Загрузка конфигурации:**
   ```bash
   # Загрузить xml/md в базу 1С
   ```

2. **Включение замера производительности:**
   - Открыть конфигуратор 1С
   - Меню "Администрирование" → "Поддержка и обслуживание" → "Замер производительности"
   - Запустить замер

3. **Запуск тестового сценария:**
   - Открыть обработку "ОбработкаТест"
   - Нажать кнопку "Тест" (основной сценарий)
   - Нажать кнопку "СтартоватьФоновое" (фоновое задание)

4. **Остановка замера:**
   - Остановить замер производительности
   - Сохранить результат в `.pff` файл

## Ожидаемый результат

PFF файл с записями, покрывающими:
- Различные контексты выполнения (Client/Server/Background)
- Переходы между контекстами (Client → Server)
- Вложенные вызовы с разными уровнями (Level)
- Специальные случаи (исключения, транзакции)

## Следующие шаги

1. Получить `.pff` файл после запуска тестов
2. Проанализировать поля Context для каждого типа вызова
3. Задокументировать точный формат в `docs/PFF_Specification.md`
4. Разработать новый парсер с корректным маппингом полей
