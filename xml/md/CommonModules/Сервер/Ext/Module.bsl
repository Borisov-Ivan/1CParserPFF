// Серверный модуль для тестирования трассировки PFF


Процедура ВыполнитьСерверныйКод() Экспорт
	// Простая серверная процедура
	Сообщить("Выполняется серверный код");
	
	// Вызов общего кода из серверного контекста
	КлиентСервер.ОбщийКод();
	
	// Цикл для создания заметной задержки
	Счетчик = 0;
	Для Индекс = 1 По 2000 Цикл
		Счетчик = Счетчик + 1;
	КонецЦикла;
КонецПроцедуры

Процедура _ВызватьИсключение() Экспорт
	// Генерация исключения для тестирования обработки ошибок в трассе
	ВызватьИсключение "Тестовое исключение для анализа PFF";
КонецПроцедуры

Процедура ТестТранзакции() Экспорт
	// Тест работы с транзакцией
	НачатьТранзакцию();
	
	Попытка
		// Выполнение операций в транзакции
		Счетчик = 0;
		Для Индекс = 1 По 500 Цикл
			Счетчик = Счетчик + 1;
		КонецЦикла;
		
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		ВызватьИсключение;
	КонецПопытки;
КонецПроцедуры

Процедура РегламентноеЗадание() Экспорт
	// Код фонового задания для тестирования контекста фоновых заданий
	
	ЗаписьЖурналаРегистрации(
		"ТестPFF.ФоновоеЗадание",
		УровеньЖурналаРегистрации.Информация,
		,
		,
		"Старт фонового задания");
	
	// Выполнение работы
	Счетчик = 0;
	Для Индекс = 1 По 3000 Цикл
		Счетчик = Счетчик + 1;
	КонецЦикла;
	
	// Вызов общего кода из фонового задания (контекст сервера)
	КлиентСервер.ОбщийКод();
	
	ЗаписьЖурналаРегистрации(
		"ТестPFF.ФоновоеЗадание",
		УровеньЖурналаРегистрации.Информация,
		,
		,
		"Завершение фонового задания");
КонецПроцедуры
//