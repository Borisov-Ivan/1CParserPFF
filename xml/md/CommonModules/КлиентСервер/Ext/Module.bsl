// Общий модуль (КлиентСервер) для тестирования трассировки PFF
// Может вызываться как с клиента, так и с сервера

Процедура ОбщийКод() Экспорт
	// Общая процедура, доступная из любого контекста
	
	// Простое вычисление для создания заметного следа в трассе
	Результат = 0;
	Для Индекс = 1 По 100 Цикл
		Результат = Результат + Индекс;
	КонецЦикла;
	
	// Вложенный вызов для проверки иерархии
	ВспомогательнаяПроцедура();
КонецПроцедуры

Процедура ВспомогательнаяПроцедура()
	// Вложенная процедура для проверки уровней вложенности в трассе
	Счетчик = 0;
	Для Индекс = 1 По 50 Цикл
		Счетчик = Счетчик + 1;
	КонецЦикла;
КонецПроцедуры